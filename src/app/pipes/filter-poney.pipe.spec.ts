import { FilterPonyPipe } from './filter-pony.pipe';

describe('FilterPonyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPonyPipe();
    expect(pipe).toBeTruthy();
  });
});
